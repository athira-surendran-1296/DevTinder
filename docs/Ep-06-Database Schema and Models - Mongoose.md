# Notes

Database, Schema and Models
==============================
* We will use popular node package "mongoose" to connect to our DB
* Install mongoose
* devTinder is the DB, user is modal
* __id --> identifier, __v --> version of the data
